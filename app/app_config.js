export default function ($rootScope) {
    'ngInject';

    $rootScope.$on('$stateChangeStart', () => {
        //
    });

    $rootScope.$on('$stateChangeSuccess', () => {
        //
    });
}
